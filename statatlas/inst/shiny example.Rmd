---
output: html_document
runtime: shiny
---

### Choosing your variables  

```{r, echo=FALSE}

shinyUI(navbarPage("Get Your Data",
  tabPanel("Single Year"),
  tabPanel("Multiple Years")
))

library(statatlas)
inputPanel(
  selectInput("year", label = "Census Year:",
              choices = 10*179:196, selected = 1790),
  
  textInput("topic", label = "Topic:", value = ""),
  
  textInput("topic2", label = "Secondary Topic:", value = "")
)

#renderTable({
#  library(statatlas)
#  data(stateslist)
#  variables <- names(stateslist[[paste0("X",input$year)]])
#  
#  data.frame(variable1 = variables[grep(input$topic, variables)])
#})

#renderTable({
#  library(statatlas)
#  data(stateslist)
#  variables <- names(stateslist[[paste0("X",input$year)]])
#  
#  data.frame(variable2 = variables[grep(input$topic2, variables)])
#})

renderTable({
  library(statatlas)
  data(stateslist)
  variables <- names(stateslist[[paste0("X",input$year)]])
  variable1 = variables[grep(input$topic, variables)]
  variable2 = variables[grep(input$topic2, variables)]
  data.frame(available_colnames = variable1[variable1 %in% variable2])
})

inputPanel(
  sliderInput("range", label = "Range of Years",
              min = 1790, max = 1960, step = 10, sep = "", value = c(1790, 1820)),
  
  selectInput("topic.choose", label = "Choose Topic",
              choices = c("Gender", "Race", "Agriculture")),
  
  selectInput("focus.choose", label = "Choose Focus Group",
              choices = c("FEMALE", "MALE", "WHITE", "NONWHITE"))
)

renderTable({
  variables <- names(stateslist[[paste0("X", input$range)]])
  
  #input$range
})
```

